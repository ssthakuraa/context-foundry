{
  "name": "@context-foundry/preparation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22"},
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js"
  },
  "dependencies": {"@context-foundry/contracts": "workspace:*", "@context-foundry/extractor-spike": "workspace:*"},
  "devDependencies": {"@types/node": "24.9.1", "typescript": "5.9.3"}
}
