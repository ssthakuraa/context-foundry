# Restart prompt for ContextFoundry

Copy the following paragraph into a new Codex task if this task cannot be resumed directly. Use the same local ContextFoundry repository.

> Continue ContextFoundry in `/home/ssthakur/projects/context-foundry`. First read `/home/ssthakur/projects/context-foundry/restartcontext.md` and `/home/ssthakur/projects/context-foundry/WORKLIST.md`, inspect Git status and applicable instructions, then follow their links to the CF-0.3 architecture review and A1–A5 implementation handoff. Verify continuity claims against the code and my latest messages. Resume the earliest incomplete A task: minimum extensible contracts, one integrated multi-artifact capture, baseline versus typed retrieval, reviewed selective investigation, then refresh correctness. Own the architecture and verification; do not simply adopt every suggestion or repeat a broad survey. Keep source-input safety, classification and provenance now; defer enterprise administration until utility is proven, without serving private knowledge or running untrusted adapters before security gates. Local simulated reviews are not authenticated approvals. Update docs, checked worklist evidence, restart context and generated HTML; commit/push reviewed public checkpoints. Reference/evaluation repos are read-only, and private material stays out of GitHub. Continue across deliverables unless a genuine authority/domain/scope decision needs me. Sol Medium may implement the specified queue; surface new architectural/security decisions for stronger review. Neither a design proposal nor passing component tests proves utility or grants human approval.

If you can resume this same task, ask Codex simply: “Continue ContextFoundry from `restartcontext.md` and `WORKLIST.md`.” A resumed task retains conversation history; a new task needs the fuller paragraph above.
